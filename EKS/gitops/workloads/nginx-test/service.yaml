apiVersion: v1
kind: Service
metadata:
  name: nginx-test
  namespace: nginx-test
  labels:
    app: nginx-test
    app.kubernetes.io/name: nginx-test
spec:
  selector:
    app: nginx-test
  ports:
    - name: http
      port: 80
      targetPort: http
      protocol: TCP
