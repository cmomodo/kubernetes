# Production-oriented Traefik overrides for chart version 29.0.1.
#
# TLS is passed through by the Network Load Balancer and terminated by Traefik
# using cert-manager-managed Kubernetes Secrets.

deployment:
  replicas: 2

podDisruptionBudget:
  enabled: true
  minAvailable: 1

updateStrategy:
  type: RollingUpdate
  rollingUpdate:
    maxUnavailable: 0
    maxSurge: 1

ingressClass:
  enabled: true
  isDefaultClass: true

# Expose Traefik through an internet-facing AWS Network Load Balancer.
service:
  enabled: true
  type: LoadBalancer
  annotations:
    service.beta.kubernetes.io/aws-load-balancer-type: "external"
    service.beta.kubernetes.io/aws-load-balancer-nlb-target-type: "ip"
    service.beta.kubernetes.io/aws-load-balancer-scheme: "internet-facing"
    external-dns.alpha.kubernetes.io/hostname: "argocd.ceedev.co.uk,nginx.ceedev.co.uk"

ports:
  web:
    port: 8000
    exposedPort: 80
    expose:
      default: true
    redirectTo:
      port: websecure
      scheme: https
      permanent: true
  websecure:
    port: 8443
    exposedPort: 443
    expose:
      default: true
  traefik:
    expose:
      default: false
  metrics:
    expose:
      default: false

# Bind the dashboard only to Traefik's non-public management entrypoint.
ingressRoute:
  dashboard:
    enabled: true
    entryPoints:
      - traefik

providers:
  kubernetesCRD:
    enabled: true
    allowCrossNamespace: false
    allowExternalNameServices: false
  kubernetesIngress:
    enabled: true
    allowExternalNameServices: false
    publishedService:
      enabled: true

logs:
  general:
    level: INFO
    format: json
  access:
    enabled: true
    format: json
    fields:
      general:
        defaultmode: keep
      headers:
        defaultmode: drop

metrics:
  prometheus:
    entryPoint: metrics
    addEntryPointsLabels: true
    addServicesLabels: true

resources:
  requests:
    cpu: "100m"
    memory: "128Mi"
  limits:
    cpu: "500m"
    memory: "512Mi"

topologySpreadConstraints:
  - maxSkew: 1
    topologyKey: kubernetes.io/hostname
    whenUnsatisfiable: DoNotSchedule
    labelSelector:
      matchLabels:
        app.kubernetes.io/name: "{{ template \"traefik.name\" . }}"
        app.kubernetes.io/instance: "{{ .Release.Name }}-{{ include \"traefik.namespace\" . }}"

securityContext:
  capabilities:
    drop:
      - ALL
  readOnlyRootFilesystem: true
  allowPrivilegeEscalation: false

podSecurityContext:
  runAsGroup: 65532
  runAsNonRoot: true
  runAsUser: 65532
  seccompProfile:
    type: RuntimeDefault

globalArguments:
  - "--global.checknewversion=false"
  - "--global.sendanonymoususage=false"
