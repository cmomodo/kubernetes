provider:
  name: aws

env:
  - name: AWS_REGION
    value: us-east-1

domainFilters:
  - ceedev.co.uk

txtOwnerId: "external-dns"

policy: sync

# Terraform associates this ServiceAccount with its IAM role using EKS Pod
# Identity. No role annotation or static AWS credentials are needed here.
serviceAccount:
  annotations: {}
