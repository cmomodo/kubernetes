provider:
  name: aws

env:
  - name: AWS_REGION
    value: us-east-1

domainFilters:
  - "${domain_name}"

txtOwnerId: "external-dns"

policy: sync

serviceAccount:
  annotations:
    eks.amazonaws.com/role-arn: "${external_dns_role_arn}"
